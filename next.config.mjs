/** @type {import('next')./** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "fakeimg.pl",
      "fakestoreapi.com"
    ],
  },
};
export default nextConfig;
